
import { supabase } from './client';

// This file is now empty as the notification bucket creation functionality has been removed
// You can delete this file if it's no longer needed elsewhere in the application

export const createNotificationsBucket = async () => {
  // Function is kept but does nothing now
  console.log("Notification bucket functionality has been removed");
  return false;
};
